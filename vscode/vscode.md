# 外部パッケージ取得時のエラー
  - エラー文
  Expected to find project root in current working directory.  
    - ターミナルの「作業ディレクトリ」がズレている。  ターミナルにて　`cd <フォルダ名>`でパッケージを使用したいフォルダに移動してから使用する。  

# フォルダ内に新しいファイルを作った際にうまくコミットできない
 - ソース管理を確認すると複数あるはず。
 　別のファイルを参照してしまっているので、Finderの隠しファイルを表示して不要なファイルを削除。再度コミットで解決する。